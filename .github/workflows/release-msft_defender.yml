name: Release msft_defender

on:
  push:
    branches:
      - main
    paths:
      - 'msft_defender/**'
  workflow_dispatch:

jobs:
  build-and-release:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'

      - name: Build binaries
        run: |
          cd msft_defender
          make deps
          make build

      - name: Upload binaries as artifacts
        uses: actions/upload-artifact@v4
        with:
          name: msft_defender-binaries
          path: |
            msft_defender/mdatp_extension-arm64.ext
            msft_defender/mdatp_extension-x86_64.ext
            msft_defender/mdatp_extension.ext

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: Fleet Extensions Release
          body: |
            This release includes updated binaries for the following extensions:

            | Extension              | Built in this release |
            |------------------------|----------------------|
            | macos_compatibility    |                      |
            | santa                  |                      |
            | system_profiler        |                      |
            | snap_packages          |                      |
            | nuget_packages         |                      |
            | brew_list              |                      |
            | msft_defender          | ✅                   |

            See attached assets for binaries.
          files: |
            msft_defender/msft_defender.ext
          draft: false
          prerelease: false

