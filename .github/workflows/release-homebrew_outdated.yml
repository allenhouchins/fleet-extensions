name: Release homebrew_outdated

on:
  push:
    branches:
      - main
    paths:
      - 'homebrew_outdated/**'
  workflow_dispatch:

jobs:
  build-and-release:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26'

      - name: Build binaries
        run: |
          cd homebrew_outdated
          make deps
          make build

      - name: Upload binaries as artifacts
        uses: actions/upload-artifact@v4
        with:
          name: homebrew_outdated-binaries
          path: |
            homebrew_outdated/homebrew_outdated-x86_64.ext
            homebrew_outdated/homebrew_outdated-arm64.ext
            homebrew_outdated/homebrew_outdated.ext

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: Fleet Extensions Release
          body: |
            This release includes updated binaries for the following extensions:

            | Extension                 | Built in this release |
            |---------------------------|----------------------|
            | apple_network_quality     |                      |
            | brew_list                 |                      |
            | homebrew_info             |                      |
            | homebrew_outdated         | ✅                    |
            | local_network_permissions |                      |
            | macos_compatibility       |                      |
            | mise                      |                      |
            | msft_defender             |                      |
            | nuget_packages            |                      |
            | santa                     |                      |
            | secureboot_cert_update    |                      |
            | snap_packages             |                      |
            | softwareupdate            |                      |
            | system_profiler           |                      |
            | ubuntu_pro                |                      |

            See attached assets for binaries.
          files: |
            homebrew_outdated/homebrew_outdated-x86_64.ext
            homebrew_outdated/homebrew_outdated-arm64.ext
            homebrew_outdated/homebrew_outdated.ext
          draft: false
          prerelease: false
