name: Release secureboot_cert_update

on:
  push:
    branches:
      - main
    paths:
      - 'secureboot_cert_update/**'
  workflow_dispatch:

jobs:
  build-and-release:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26'
          cache-dependency-path: secureboot_cert_update/go.sum

      - name: Build Windows binaries
        run: |
          cd secureboot_cert_update
          make deps
          make build

      - name: Upload binaries as artifacts
        uses: actions/upload-artifact@v4
        with:
          name: secureboot_cert_update-binaries
          path: |
            secureboot_cert_update/secureboot_cert_update-amd64.exe
            secureboot_cert_update/secureboot_cert_update-arm64.exe

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: Fleet Extensions Release
          body: |
            This release includes updated binaries for the following extensions:

            | Extension                 | Built in this release |
            |---------------------------|----------------------|
            | apple_network_quality     |                      |
            | brew_list                 |                      |
            | homebrew_info             |                      |
            | homebrew_outdated         |                      |
            | local_network_permissions |                      |
            | macos_compatibility       |                      |
            | mise                      |                      |
            | msft_defender             |                      |
            | nuget_packages            |                      |
            | santa                     |                      |
            | secureboot_cert_update    | ✅                    |
            | snap_packages             |                      |
            | softwareupdate            |                      |
            | system_profiler           |                      |
            | ubuntu_pro                |                      |

            See attached assets for binaries.
          files: |
            secureboot_cert_update/secureboot_cert_update-amd64.exe
            secureboot_cert_update/secureboot_cert_update-arm64.exe
          draft: false
          prerelease: false
